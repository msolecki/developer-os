{
  "name": "developer-os",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first workflows and an Obsidian-compatible Brain for Claude Code and Codex",
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=24.16.0 <25"
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --pretty false && eslint . && node tests/dist/repository/check.js",
    "test": "npm run test:bootstrap && npm run test:suite",
    "test:bootstrap": "vitest run apps/cli/src/bootstrap/executor.test.ts -t 'publishes a complete V2 handoff and permanently retains its exact plan and two slots' && vitest run apps/cli/src/bootstrap/executor.test.ts -t '^(?!.*publishes a complete V2 handoff and permanently retains its exact plan and two slots).*$'",
    "test:suite": "vitest run --exclude 'src/bootstrap/executor.test.ts' --exclude 'e2e/**' --exclude 'integration/ingest/no-user-hooks.test.ts'",
    "test:e2e": "vitest run tests/e2e",
    "test:vendor-ingest": "vitest run tests/integration/ingest/no-user-hooks.test.ts",
    "render:claude": "tsc -b && node tests/dist/tools/render-claude.js",
    "render:codex": "tsc -b && node tests/dist/tools/render-codex.js",
    "check": "npm run lint && npm test && npm run test:e2e && npm run test:vendor-ingest && pnpm --pm-on-fail=ignore build && git diff --check"
  },
  "devDependencies": {
    "@eslint/js": "9.39.4",
    "@types/node": "22.19.19",
    "eslint": "9.39.4",
    "typescript": "5.9.3",
    "typescript-eslint": "8.60.1",
    "vitest": "4.1.8"
  }
}
